## 1. プロジェクトのセットアップ

- [x] 1.1 Dockerfile および docker-compose.yml の作成による、Node.jsコンテナ環境の構築
- [x] 1.2 Dockerコンテナ内でのAstroプロジェクトの初期化およびパッケージ構成の整備 (Alpine.js integration の追加)
- [x] 1.3 Dockerコンテナ内でのHono バックエンドの構成と、Neon (PostgreSQL) 接続用のライブラリ/SDKのセットアップ
- [x] 1.4 データベースのテーブル作成用スキーマスクリプトの用意とコンテナ経由での初期実行（`scores` および `question_logs` テーブル）
- [x] 1.5 `public/sounds/` 内への MP3 音声ファイル群の配置、および `public/images/` 内への画像アセット群（女教師、各ステージ鬼画像）の配置

## 2. バックエンドAPIの実装 (Hono)

- [x] 2.1 セッショントークン（暗号化トークン）の生成・署名・復号ユーティリティの実装（Web Crypto API等を使用）
- [x] 2.2 ステージ開始時の初期5問生成とトークン発行 `POST /api/stages/start` の実装（ステージ1〜12の難易度規則の組み込み）
- [x] 2.3 4問目回答時の追加5問生成とトークン更新 `POST /api/stages/more` の実装
- [x] 2.4 ステージクリア（またはゲームオーバー）時の解答・時間整合性検証、およびスコア保存・出題ログ（`question_logs`）の非同期一括インサートの実装 (`POST /api/stages/clear`)
- [x] 2.5 スコアボード（ランキング）を取得する `GET /api/scores` エンドポイントの実装
- [ ] 2.6 Dockerコンテナ内でのローカル動作確認および Vercel Serverless 上での動作確認テスト

## 3. フロントエンド基礎とUIコンポーネント (Astro)

- [x] 3.1 プレミアムなダークモードテーマ用 CSS / デザインシステムの構築
- [x] 3.2 ゲームメイン画面の基本レイアウトの作成 (バトルエリア、問題表示エリア、キーパッドエリア、スコアボードエリア)
- [x] 3.3 `public/images/` に配置した女教師および鬼の画像アセットの表示処理の実装
- [x] 3.4 Astroの `ViewTransitions` 統合と、ページをまたいでBGM要素を永続化（persist）するためのレイアウト設計

## 4. ゲームプレイロジックの実装 (Alpine.js)

- [x] 4.1 ゲーム開始・進行・ゲームオーバー（ライフが0になる）の状態管理ロジックの実装 (初期値ハート3個)
- [x] 4.2 非同期での問題取得キューの実装（初期5問のロード、4問目回答開始時に裏で次の5問を取得、トークンの更新）
- [x] 4.3 テンキーボタンおよびキーボード入力（数字、Backspace、Enter）の入力受付処理
- [x] 4.4 攻撃時のダメージエフェクト（鬼の揺れ等）および被ダメージ時のエフェクト（毒攻撃、ハート減少・破損アニメーション）のCSS/JS実装
- [x] 4.5 ステージクリア・進行およびスコア・ダメージ計算の実装（基本得点・タイムボーナス・鬼のHP・鬼ダメージ量・鬼撃破ボーナスの計算、ライフ全回復）
- [x] 4.6 Web Audio API を用いた効果音の事前フェッチとデコード・キャッシュ処理の実装（低遅延再生用）
- [x] 4.7 スタート画面でのユーザー操作（クリック等）を契機にした BGM 再生開始と AudioContext 再開処理の実装

## 5. 必殺技ゲージ・カットイン演出・バトル画面レイアウト

- [x] 5.1 必殺技ゲージのUI実装（正解ごとにゲージ蓄積、決定論的加算）
- [x] 5.2 必殺技発動ロジックの実装（ゲージ100%で3倍ダメージ、発動後ゲージリセット）
- [x] 5.3 スラッシュ風カットイン演出のCSS/JS実装（カットイン完了後に攻撃演出へ遷移）
- [x] 5.4 バトル画面レイアウト刷新（左上=女教師/右上=鬼/中央=スコア、レスポンシブ対応）
- [x] 5.5 鬼画像を女教師より大きく表示・透過余白のトリミング処理
- [x] 5.6 正解時の光玉ゲージ演出（白色発光の光玉が女教師へ集約しゲージを進行、全インライン描画でスクロール非干渉）
- [x] 5.7 正解時の女教師→鬼ビーム演出と着弾同期ダメージ、不正解時の鬼→女教師毒玉演出（小玉が公転して纏わりつく）と着弾同期ライフ減少、着弾までの入力ロック
- [x] 5.8 必殺技専用効果音（`必殺技.mp3`）の登録と発動時再生
- [x] 5.9 必殺技演出の効果音整理: カットイン開始時に`必殺技カットイン.mp3`、✕エフェクト着弾時に`必殺技.mp3`を再生し、通常のビーム砲・正解効果音の重複再生を排除

## 6. API連携とスコアボード表示の統合

- [x] 6.1 ゲームオーバー時のスコア保存API呼び出しの実装
- [x] 6.2 データベースから取得したランキング（スコアボード）のゲーム画面への描画処理
- [ ] 6.3 全体的なUI・操作性の最終的な磨き上げ（Docker/Vercel上での実機確認後にチェック）

## 7. ニックネーム入力とランキング登録フロー

- [x] 7.1 スタート画面の改修: ニックネーム入力欄を撤去し、ボタンのみでゲスト即時開始にする
- [x] 7.2 スコア保存の再設計: `POST /api/stages/clear` での自動保存を廃止し、検証結果と最終スコア・結果トークンの返却のみにする
- [x] 7.3 重複排除用の永続化を追加: `scores` への `result_hash`（UNIQUE）列追加、または `submitted_results` テーブル新設と、既存DB向けマイグレーションSQL（`init.sql` 更新）
- [x] 7.4 ランキング登録API `POST /api/scores/submit` の実装（結果トークン＝`session_token`＋解答ログを再検証→重複排除キーの一意制約で二重登録を拒否→表示名付きで `scores` に保存）
- [x] 7.5 ゲームオーバー／ALLクリア画面にニックネーム入力UIと「ランキングに登録」導線を実装（未入力時は `「ゲスト-」＋player_id末尾4文字` を表示名に）
- [x] 7.6 同一セッション中の入力ニックネーム記憶（`sessionStorage`／Alpineストア）と入力UIへの初期表示
- [x] 7.7 表示名のサニタイズ（トリム・文字数上限・制御文字除去）の実装
- [x] 7.8 登録フローの動作確認（再検証でスコア改ざん不可・未入力時の表示名整形・同一結果の二重登録拒否・登録せず離脱でスコア非保存）

## 8. スタート画面のTOPビジュアル刷新

- [x] 8.1 TOPバナー画像の配置（`public/images/top-mobile.png` / `public/images/top-pc.png`）
- [x] 8.2 スタート画面をバナー主体のデザインへ刷新（`<picture>`で768px境界のモバイル/PC出し分け、重複テキストタイトル撤去・`sr-only`見出し保持）
- [x] 8.3 バナーを大きく表示し常時アニメーションを廃止（揺れ・浮遊・シーン演出を除去して静止表示）、開始ボタンをバナー画像上に重ね配置。ボタンは点滅をやめ、定期的に光が走る「キラーン」演出（`btn-shine`）にする
- [x] 8.4 アプリ名称を「鬼と学ぼう！算数アドベンチャー」に統一（ヘッダー／ページtitle／OGP／`site.webmanifest`／MediaSession）
- [x] 8.5 UIカラーテーマを深緑・非グラデーション・要素別配色へ刷新（背景・ヘッダー・パネル・ボタン・テンキー・テキスト）。バトル演出の配色・グラデーションは維持
- [x] 8.6 薄緑案を一時検討するも不採用とし、深緑テーマで確定（背景=深緑・文字=明緑/白系）

## 9. 鬼撃破時の問題表示クリアとランキング画面の改修

- [x] 9.1 鬼撃破確定時に問題表示をクリア（次ステージの鬼・問題が出るまで直前の問題を残さず`?`表示）
- [x] 9.2 スコア登録API（`POST /api/scores/submit`）が登録レコードのID（`score_id`）を返却（重複時も既存IDを返す）
- [x] 9.3 ゲーム終了画面からランキングへ遷移する際、登録結果のIDを`?r=<score_id>`で渡す
- [x] 9.4 ランキング画面のトップ10をスクロールなしで全件表示
- [x] 9.5 `?r=<score_id>`付き遷移時のみ、今回プレイの順位＋前後3レコードを表示し当該レコードを強調（通常導線ではUI変更なし）
- [x] 9.6 結果遷移時の画面並びを「今回のプレイ → スタート画面に戻るボタン → TOP10ランキング」の順に変更
